# Stripe Configuration
STRIPE_SECRET=sk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

# Database Configuration
DATABASE_URL=postgres://username:password@host:port/database

# Payment Configuration (in cents)
PLATFORM_FEE_CENTS=5000
PROVIDER_CUT_CENTS=12000

# Timezone for daily transfers
TIMEZONE=America/New_York

# Server Configuration
PORT=3000

# Domain Configuration (update with your actual domain)
DOMAIN=https://yourdomain.com

# SMS Configuration Options
# Option 1: Use your existing SMS system (Recommended)
SMS_BRIDGE_URL=https://your-booking-system.com/api/send-sms
SMS_BRIDGE_TOKEN=your_api_token_if_needed

# Option 2: Direct TextMagic (Fallback - only if you have separate account)
TEXTMAGIC_USERNAME=your_textmagic_username
TEXTMAGIC_API_KEY=your_textmagic_api_key
